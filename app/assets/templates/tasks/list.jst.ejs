<h1>All Tasks</h1>
<a href="#tasks/new">New Task</a>
<ul>
  <% _(tasks).each(function (task) { %>
    <li class="task">
      <a href="#/tasks/<%= task.id %>">
        <%= task.title %>
      </a>
    </li>
  <% }); %>
</ul>